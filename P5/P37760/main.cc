#include <iostream>
#include <cmath>
using namespace std;

double dist_or(double x, double y) {
    
}

int main () {
    cout.setf(ios::fixed);
    cout.precision(6);
    double d;
    while (cin >> d) cout << sin((M_PI*d)/ 180) << ' ' << cos((M_PI*d)/ 180) << endl;
}